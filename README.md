Run as non root random user and have ssh, etc commands still work
==================================================================

Quick way of setting a valid user, was using nss_wrapper, but this works not
sure if this is better or worse
